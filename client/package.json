{
  "name": "internet-xplorer",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && netlify deploy -d dist",
    "deploy-prod": "npm run deploy -- -p"
  },
  "dependencies": {
    "@maptiler/geocoding-control": "^0.0.90",
    "bootstrap": "^5.2.3",
    "maplibre-gl": "^3.0.1",
    "pinia": "^2.0.28",
    "pmtiles": "^2.8.0",
    "vue": "^3.2.45"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.0.0",
    "vite": "^4.0.0"
  }
}
